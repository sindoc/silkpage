#  ============================================================================
#   Build property file for cdn.lutino.io (static asset CDN subtree)
#   No SilkPage XML pipeline — assets are copied verbatim.
#  ============================================================================
default.encoding: UTF-8
default.uid:      ${user.name}
default.temp.dir: ${java.io.tmpdir}
default.url:      cdn.lutino.io

dist.dir:  ${temp.dir}/${url}/dist
www.dir:   ${user.home}/var/deploy/cdn.lutino.io

uid:       ${default.uid}
temp.dir:  ${default.temp.dir}
url:       ${default.url}
