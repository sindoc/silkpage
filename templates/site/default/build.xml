<?xml version="1.0"?>
<!--
  =======================================================================
  Build instruction of @site.id@, Generated by SilkPage.
  To build the site for preview, type in 'ant preview' on the command line
  For Online documentation, http://silkpage.markupware.com/docs/index.html
  =======================================================================
-->

<project name="@site.id@" default="preview">
<property name="silkpage.home.dir" value="@silkpage.home.dir@"/>

 <!--
       ===================================================================
       preview        		     
       ===================================================================
  -->
<target name="preview"
   description="generates and installs a local copy for preview">
   <ant antfile="@silkpage.home.dir@/src/xml/build/tasks.xml"
     dir="${silkpage.home.dir}" inheritAll="false" target="installLocal">
     <property file="build.properties"/>
   </ant>
</target>
 <!--
       ===================================================================
       publish
       ===================================================================
  -->
<target name="publish" depends="preview"
   description="publishes a copy of your website online">
   <ant antfile="${silkpage.home.dir}/src/xml/build/tasks.xml"
     dir="${silkpage.home.dir}" inheritAll="false" target="installRemote">
     <property file="build.properties"/>
   </ant>
</target>

<!--
       ===================================================================
       feeds
       ===================================================================
  -->
<target name="feeds" description="generates required feeds based on feed configuration file">
  <exec executable="xsltproc">
    <arg line="--stringparam user.feeds.basedir ${user.feeds.basedir}"/>
    <arg line="${silkpage.home.dir}/src/xsl/common/feeds-config.xsl"/>
    <arg line="${user.feeds.configfile}"/>
  </exec>

</target>


</project>
